<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #4cc9f0;">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Shopping Cart.com</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

        <!-- paste here -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <% if(!currentUser) {%>
                        <a class="nav-link active" href="/products">Products</a>
                        <% } else{%>
                            <% if(currentUser.userType=='retailer'){ %>
                                <a class="nav-link active" href="/retailer/<%=currentUser._id%> ">Products</a>
                                <% }else {%>
                                    <a class="nav-link active" href="/products">Products</a>
                                    <%}  %>
                                        <% } %>
                </li>
                <% if(currentUser){%>
                    <% if(currentUser.userType=='retailer'){ %>
                        <li class="nav-item">
                            <a class="nav-link active" href="../products/new" tabindex="-1" aria-disabled="true">New</a>
                        </li>
                        <% } %>

                            <% } %>
            </ul>
            <div class="navbar-nav ml-auto">
                <% if(!currentUser){ %>
                    <a href="/login" class="nav-link active">Login   <i class="fas fa-user"></i></a>
                    <% } else{%>


                        <a href="/user/<%= currentUser._id %>/cart " class="nav-link active"><i class="fas fa-shopping-cart"></i></a>
                        <sup><span class="badge bg-secondary"><%= currentUser.cart.length %></span></sup>

                        <a class="nav-link active text-capitalize">Hello <%=currentUser.username %> </a>

                        <a class="nav-link active" href="/profile"><i class="far fa-user"></i></a><a href="/logout" class="nav-link active">Logout </a>
                        <% } %>

            </div>
        </div>
        <!-- to  here -->

    </div>
</nav>